{
  "version": 2,
  "builds": [
    {
      "src": "api/*.js",
      "use": "@vercel/node"
    },
    {
      "src": "build/**/*",
      "use": "@vercel/static"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },    
    { "src": "/manifest.json", "dest": "/build/manifest.json" },     
    { "handle": "filesystem" },                        
    { "src": "/(.*)", "dest": "/build/index.html" }  
  ]
}